<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cardworld">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CardWorld">
        <activity android:name=".MarriageGreeting"></activity>
        <activity
            android:name=".NewYearGreeting"
            android:label="@string/title_activity_new_year_greeting"
            android:theme="@style/Theme.CardWorld.NoActionBar" />
        <activity android:name=".Birthdaygreeting" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>